# Local Environment Configuration
# Copy this to .env.local and fill in your values
# DO NOT commit .env.local to git!

# Required: Linode API Token
# Get from: https://cloud.linode.com/profile/tokens
LINODE_PAT=your_linode_personal_access_token_here

# Required: Root password for the VPN server (min 8 characters)
ROOT_PASSWORD=YourSecurePassword123!

# Required: Your SSH public key (will be auto-detected if not set)
# Get with: cat ~/.ssh/id_ed25519.pub or cat ~/.ssh/id_rsa.pub
SSH_PUBLIC_KEY=ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAA... your@email.com

# Optional: Email notifications (for local testing)
# Note: Email is automatically sent via GitHub Actions workflow
MAIL_USERNAME=your.email@gmail.com
MAIL_PASSWORD=your_gmail_app_password
MAIL_TO=recipient@email.com

# Optional: Deployment configuration
TF_VAR_region=us-east
TF_VAR_instance_type=g6-nanode-1
